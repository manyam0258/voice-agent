# =============================================================================
# Voice Agent — Environment Variables
# Copy this file to .env and fill in your real values.
# NEVER commit your .env file to version control.
# =============================================================================

# -----------------------------------------------------------------------------
# LiveKit Cloud
# Get these from: https://cloud.livekit.io → your project → Settings → Keys
# -----------------------------------------------------------------------------
LIVEKIT_URL=wss://your-project.livekit.cloud
LIVEKIT_API_KEY=your_livekit_api_key
LIVEKIT_API_SECRET=your_livekit_api_secret

# -----------------------------------------------------------------------------
# Deepgram — Speech-to-Text
# Get from: https://console.deepgram.com → API Keys
# -----------------------------------------------------------------------------
DEEPGRAM_API_KEY=your_deepgram_api_key

# -----------------------------------------------------------------------------
# OpenAI — Language Model
# Get from: https://platform.openai.com → API Keys
# -----------------------------------------------------------------------------
OPENAI_API_KEY=your_openai_api_key

# Model to use — gpt-4o-mini is cheapest, gpt-4o is most capable
OPENAI_MODEL=gpt-4o-mini

# -----------------------------------------------------------------------------
# ElevenLabs — Text-to-Speech
# Get from: https://elevenlabs.io → Profile → API Key
# -----------------------------------------------------------------------------
ELEVENLABS_API_KEY=your_elevenlabs_api_key

# Voice ID — find IDs in ElevenLabs voice library or My Voices
# Default below is "Rachel" (a clear, friendly voice)
ELEVENLABS_VOICE_ID=21m00Tcm4TlvDq8ikWAM

# -----------------------------------------------------------------------------
# Twilio — Inbound Phone Calls via SIP
# Get from: https://console.twilio.com → Account Info
# Used ONLY for webhook request validation (security)
# -----------------------------------------------------------------------------
TWILIO_AUTH_TOKEN=your_twilio_auth_token

# -----------------------------------------------------------------------------
# Supabase — Call Log Storage
# Self-hosted (Coolify): Use your custom domain URL
# Supabase Cloud: Use the URL from Settings → API
# Use the SERVICE ROLE key (not anon key) — it has write permissions
# -----------------------------------------------------------------------------
SUPABASE_URL=https://supabase.yourdomain.com
SUPABASE_SERVICE_KEY=your_supabase_service_role_key

# -----------------------------------------------------------------------------
# Agent Behavior
# -----------------------------------------------------------------------------

# Maximum call duration in seconds before agent hangs up (cost protection)
# 600 = 10 minutes. Adjust based on your expected call length.
MAX_CALL_DURATION_SECONDS=600

# System prompt — defines the agent's personality and purpose
# Customize this to match your use case
AGENT_SYSTEM_PROMPT=You are a helpful, friendly voice assistant answering inbound phone calls. Be concise — phone callers prefer short, clear answers. If you don't know something, say so honestly rather than guessing.
